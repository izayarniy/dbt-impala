[project]
name = "mgt-dbt-impala"
version = "0.1.0"
description = "mgt fork for abandoned dbt-impala adapter"
readme = "README.md"
authors = [
    { name = "Ivan Zayarniy", email = "izayarniy@gmail.com" }
]
requires-python = "==3.12.12"

#dependencies = [
##  "base>=0.0.0",
#  "dbt-core==1.11.5",
##  "dbt-adapters==1.22.5",
#  "impyla==0.22.0",
#        "python-decouple==3.6",
#    "pytest>=7.0",
#    "dbt-tests-adapter==1.10.4",
#    "tox~=4.11",
#    "pytest",
#    "pytest-xdist~=3.5",
#    "pre-commit~=3.2",
#    "pytest-dotenv==0.5.2",
#    "python-dotenv==1.2.1"
#]

#
#[tool.setuptools.packages.find]
#where = [".ddd"] # Tells setuptools to look for packages inside the 'src' directory

[tool.uv.build-backend]
module-name = "dbt"
module-root = "."

[project.optional-dependencies]
dev = [
#    "dbt-utils",

#    "python-decouple==3.6",
#    "pytest>=7.0",
#    "dbt-tests-adapter==1.10.4",
#    "tox~=4.11",
#    "pytest",
#    "pytest-xdist~=3.5",
#    "pre-commit~=3.2",
#    "pytest-dotenv==0.5.2",
#    "python-dotenv==1.2.1"
#    "pytest-cov", # For coverage reports
]

[project.scripts]
dbt-demo = "dbt_demo:main"

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"
